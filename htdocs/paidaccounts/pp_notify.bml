<?_code
{

	use strict;
	use vars qw( %FORM );
	
	# we shouldn't actually NEED notifications from PayPal for this fairly simple
	# payment system to work fine.  but we're going to log them anyway and then
	# see what kind of content they have just in case we actually need this
	# information for some reason or another.

	DW::Pay::pp_log_notify( \%FORM );

	return "notified";

}
_code?>
