type: object
required:
  - name
properties:
  name:
    description: The name of the new accesslist.
    type: string
    minLength: 1
  sortorder:
    description: Sort order for the new accesslist, 0 to 255.
    type: integer
    minimum: 0
    maximum: 255
