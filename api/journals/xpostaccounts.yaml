---
paths:
  /journals/{username}/xpostaccounts:
    parameters:
      - name: username
        in: path
        description: The username you want crosspost information for
        type: string
        required: true
    get:
      description: Returns a list of crosspost identities for the given journal
      responses:
        200:
          description: A list of tags.