---
paths:
  /spec:
    get:
      description: Returns the API specification
      responses:
        200:
          description: This API specification!
        500:
          $ref: components/errors/500.yaml