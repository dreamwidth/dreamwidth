---
paths:
  /users/{username}/subscriptions:
    parameters:
      - name: username
        in: path
        description: The journal you want entry information for
        type: string
        required: true    
    get:
      description: Returns a list of journals the username subscribes to.
      responses:
        200:
          description: A list of journal names.