#!/usr/bin/perl

use strict;
use lib "$ENV{LJHOME}/cgi-bin";

require 'ljlib.pl';

use LJ::Worker::TheSchwartz;
use DW::Worker::PaidStatus;

schwartz_decl( "DW::Worker::PaidStatus" );

schwartz_work();
