#!/usr/bin/perl
use strict;
use lib "$ENV{LJHOME}/cgi-bin";
require 'ljlib.pl';
require 'ljprotocol.pl';
use LJ::Lang;
use LJ::Worker::TheSchwartz;
use LJ::ESN;

foreach my $classname (LJ::ESN->schwartz_capabilities) {
    schwartz_decl($classname, 'mass');
}

schwartz_work();

